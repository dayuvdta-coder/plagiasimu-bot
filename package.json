{
  "name": "turnitin-web-automation",
  "version": "0.1.0",
  "private": true,
  "description": "Local web app to automate Turnitin submissions across a shared account pool.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "stress": "node scripts/stress-jobs.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "multer": "^2.0.2",
    "playwright": "^1.52.0",
    "qrcode": "^1.5.4"
  }
}
